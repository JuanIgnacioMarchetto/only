
class AuthService {
    async login(email, password) {
      // Lógica de autenticación (puede ser una llamada a un servidor)
      // Retorna una promesa que resuelve con los datos del usuario autenticado
    }
  
    // Agrega funciones adicionales para registro, cierre de sesión, etc.
  }
  
  export default new AuthService();
  